---
phase: 04-ausencias-y-cobertura
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - app/(protected)/ausencias/page.tsx
  - components/ausencias/AusenciasForm.tsx
  - components/ausencias/AusenciasList.tsx
autonomous: true
requirements:
  - ABS-01
  - ABS-02
  - ABS-03
  - ABS-04

must_haves:
  truths:
    - "La administradora puede registrar vacaciones eligiendo empleado, tipo VAC, fecha inicio y fecha fin"
    - "La administradora puede registrar INC, PER o CAL eligiendo empleado y fecha inicio (fin = mismo dia si no se especifica)"
    - "La lista del mes muestra todas las novedades con nombre del empleado, tipo con color, y rango de fechas"
    - "El boton Eliminar borra una novedad individual de la lista sin recargar la pagina"
    - "El boton Limpiar Novedades del Mes elimina todas las ausencias del mes seleccionado con confirmacion"
  artifacts:
    - path: "app/(protected)/ausencias/page.tsx"
      provides: "Server Component que carga empleados y ausencias del mes actual"
      min_lines: 30
    - path: "components/ausencias/AusenciasForm.tsx"
      provides: "Client Component con formulario para crear nueva novedad"
      min_lines: 60
    - path: "components/ausencias/AusenciasList.tsx"
      provides: "Client Component con tabla de novedades del mes y boton limpiar"
      min_lines: 60
  key_links:
    - from: "components/ausencias/AusenciasForm.tsx"
      to: "lib/actions/ausencias.ts"
      via: "Server Action createAbsence en form submit"
      pattern: "createAbsence"
    - from: "components/ausencias/AusenciasList.tsx"
      to: "lib/actions/ausencias.ts"
      via: "Server Actions deleteAbsence y clearMonthAbsences"
      pattern: "deleteAbsence|clearMonthAbsences"
---

<objective>
Vista de gestion de ausencias: formulario para registrar nuevas novedades y lista del mes con eliminacion individual y limpieza total.

Purpose: La administradora necesita una pagina dedicada para gestionar todas las novedades (vacaciones con rango de fechas, incapacidades, permisos, calamidades) independientemente de la planilla principal.

Output: 3 archivos — 1 Server Component (page), 2 Client Components (form + list).
</objective>

<execution_context>
@/Users/franciatrasvina/.claude/get-shit-done/workflows/execute-plan.md
@/Users/franciatrasvina/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-ausencias-y-cobertura/04-01-SUMMARY.md
@prisma/schema.prisma
@components/habilidades/HabilidadesTable.tsx
@components/layout/Navbar.tsx
@app/globals.css
</context>

<interfaces>
<!-- Tipos exportados por 04-01 que el ejecutor necesita (ver 04-01-SUMMARY.md para confirmacion) -->

```typescript
// lib/actions/ausencias.ts (creado en 04-01)
export type AbsenceType = "VAC" | "INC" | "PER" | "CAL" | "DESCANSO";

export interface CreateAbsenceInput {
  employeeId: number;
  type: AbsenceType;
  startDate: string; // "YYYY-MM-DD"
  endDate: string;   // "YYYY-MM-DD"
}

export async function createAbsence(input: CreateAbsenceInput): Promise<void>;
export async function deleteAbsence(id: number): Promise<void>;
export async function clearMonthAbsences(mes: number, anio: number): Promise<void>;
```

Colores semanticos para tipos de novedad (mantener consistencia con planilla):
- VAC: verde (bg-green-500/20 text-green-400 border-green-500/40)
- INC: amarillo (bg-yellow-500/20 text-yellow-400 border-yellow-500/40)
- PER: azul (bg-blue-500/20 text-blue-400 border-blue-500/40)
- CAL: naranja (bg-orange-500/20 text-orange-400 border-orange-500/40)
- DESCANSO: gris (bg-gray-500/20 text-gray-400 border-gray-500/40)

Estilo del proyecto: dark mode, glassmorphism, fondo oscuro con gradientes.
Referencia de estilos: app/globals.css + componentes existentes en components/habilidades/HabilidadesTable.tsx
</interfaces>

<tasks>

<task type="auto">
  <name>Tarea 1: Pagina de Ausencias y formulario de registro</name>
  <files>app/(protected)/ausencias/page.tsx, components/ausencias/AusenciasForm.tsx</files>
  <action>
    Crear app/(protected)/ausencias/page.tsx como Server Component:
    - Calcular mes y anio actuales con new Date() en TZ America/Bogota (usar Intl.DateTimeFormat o getFullYear/getMonth)
    - Consultar prisma.employee.findMany({ orderBy: { name: "asc" } }) para el selector de empleados
    - Consultar prisma.absence.findMany({ where: { startDate: { gte: startOfMonth, lt: endOfMonth } }, include: { employee: true }, orderBy: { startDate: "asc" } }) para la lista del mes
    - Renderizar un <h1> con "Novedades — [Mes actual en español]" (ej: "Novedades — Marzo 2026")
    - Renderizar <AusenciasForm employees={employees} />
    - Renderizar <AusenciasList absences={absences} mes={mes} anio={anio} />
    - Agregar link "Ausencias" al navbar (components/layout/Navbar.tsx) si no existe

    Crear components/ausencias/AusenciasForm.tsx como Client Component ("use client"):
    - Props: { employees: Array<{ id: number; name: string }> }
    - Campos del formulario:
      1. Select "Empleado" — lista de employees ordenados, value = employeeId
      2. Select "Tipo" — opciones VAC, INC, PER, CAL con etiquetas: Vacaciones, Incapacidad, Permiso, Calamidad
      3. Input date "Fecha inicio" (required)
      4. Input date "Fecha fin" — si tipo != VAC, precompletar automaticamente con el valor de inicio al cambiar inicio (puede dejarse igual para ausencias de un dia)
    - Al enviar: llamar createAbsence(input) del Server Action importado desde lib/actions/ausencias
    - Usar useTransition para mostrar estado pending en el boton "Registrar Novedad"
    - Limpiar el formulario al completarse exitosamente
    - Estilos glassmorphism: fondo con bg-white/5 backdrop-blur border border-white/10 rounded-xl p-6
    - Labels en espanol, inputs con bg-white/10 border-white/20 rounded-lg text-white
  </action>
  <verify>
    <automated>cd /Users/franciatrasvina/Projects/GestionTurnos && npx tsc --noEmit</automated>
  </verify>
  <done>TypeScript compila. La pagina /ausencias es accesible en la ruta protegida con el formulario visible.</done>
</task>

<task type="auto">
  <name>Tarea 2: Lista de novedades del mes con eliminar y limpiar</name>
  <files>components/ausencias/AusenciasList.tsx</files>
  <action>
    Crear components/ausencias/AusenciasList.tsx como Client Component ("use client"):

    Props:
    ```typescript
    interface AbsenceRow {
      id: number;
      type: string;
      startDate: Date;
      endDate: Date;
      employee: { name: string };
    }
    interface AusenciasListProps {
      absences: AbsenceRow[];
      mes: number;
      anio: number;
    }
    ```

    Tabla con columnas: Empleado | Tipo | Inicio | Fin | Acciones

    Formato de fechas: usar toLocaleDateString("es-CO", { day: "numeric", month: "short", year: "numeric" }) — producira "1 mar. 2026"

    Badge de tipo con colores semanticos:
    - VAC → bg-green-500/20 text-green-400 border border-green-500/40 → label "VACACIONES"
    - INC → bg-yellow-500/20 text-yellow-400 border border-yellow-500/40 → label "INCAPACIDAD"
    - PER → bg-blue-500/20 text-blue-400 border border-blue-500/40 → label "PERMISO"
    - CAL → bg-orange-500/20 text-orange-400 border border-orange-500/40 → label "CALAMIDAD"
    - DESCANSO → bg-gray-500/20 text-gray-400 border border-gray-500/40 → label "DESCANSO"

    Boton eliminar por fila: icono de papelera (o texto "Eliminar"), llama deleteAbsence(id) con useTransition. Mostrar estado pending en la fila que se esta eliminando.

    Boton "Limpiar Novedades del Mes" en el pie de la tabla:
    - Estilo rojo (bg-red-500/20 text-red-400 hover:bg-red-500/30)
    - Al hacer clic, mostrar confirm() nativo: "¿Eliminar todas las novedades de [mes en espanol] [anio]? Esta accion no se puede deshacer."
    - Solo si el usuario confirma, llamar clearMonthAbsences(mes, anio)
    - Deshabilitar durante la accion con useTransition

    Si absences.length === 0: mostrar mensaje "No hay novedades registradas para este mes." en celda colspan=5.

    Estilos: tabla con glassmorphism bg-white/5 backdrop-blur, headers en text-gray-400 uppercase text-xs, filas con border-b border-white/10 hover:bg-white/5.
  </action>
  <verify>
    <automated>cd /Users/franciatrasvina/Projects/GestionTurnos && npx tsc --noEmit</automated>
  </verify>
  <done>TypeScript compila. AusenciasList renderiza tabla con badges de color, boton eliminar por fila, y boton limpiar mes con confirmacion.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` pasa sin errores
- /ausencias es una ruta protegida bajo (protected)/ausencias/page.tsx
- Los tres componentes existen y tipan correctamente
- Badge de tipo usa colores semanticos por tipo de novedad
- Boton limpiar tiene confirmacion antes de ejecutar
</verification>

<success_criteria>
La pagina de Ausencias esta completa cuando:
1. La administradora puede navegar a /ausencias desde el navbar
2. El formulario permite seleccionar empleado, tipo (VAC/INC/PER/CAL), fecha inicio y fin
3. Al guardar, la novedad aparece en la lista sin recargar la pagina (revalidatePath la actualiza)
4. Cada fila tiene un boton eliminar que borra esa novedad
5. El boton "Limpiar Novedades del Mes" pide confirmacion y elimina todas al confirmar
6. Los badges de tipo usan colores semanticos consistentes con la planilla
</success_criteria>

<output>
Despues de completar este plan, crear `.planning/phases/04-ausencias-y-cobertura/04-02-SUMMARY.md` documentando: archivos creados, props de cada componente, colores usados por tipo de novedad, y cualquier decision de UX.
</output>
