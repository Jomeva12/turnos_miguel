---
phase: 03-planilla-principal
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - app/(protected)/planilla/page.tsx
  - components/planilla/MonthNavigator.tsx
  - components/planilla/PlanillaToolbar.tsx
  - components/planilla/PlanillaGrid.tsx
  - components/planilla/ShiftCell.tsx
  - lib/actions/shifts.ts
autonomous: true
requirements:
  - PLAN-01
  - PLAN-02
  - PLAN-03
  - PLAN-04
  - PLAN-05
  - PLAN-06
  - PLAN-07
  - PLAN-08
  - PLAN-09
  - PLAN-10
  - PLAN-11
must_haves:
  truths:
    - "La tabla cronograma muestra filas de empleados y columnas de días del mes en la ruta /planilla"
    - "Los domingos tienen encabezado rojo y el día actual está marcado visualmente"
    - "Cada celda muestra el horario del turno (o 'DESC' si es descanso), badge del área con color, e ícono ✏️ si es manual"
    - "Los filtros por área y tipo de novedad ocultan/muestran filas sin recargar la página"
    - "El selector de mes navega entre meses via query params (?mes=3&anio=2025)"
    - "Los botones Generar Mes, Asignación Manual, Excel, Limpiar Turnos y Limpiar Novedades están presentes en el toolbar"
  artifacts:
    - path: "app/(protected)/planilla/page.tsx"
      provides: "RSC shell que carga datos del mes y pasa props al grid"
      exports: ["default function PlanillaPage"]
    - path: "components/planilla/PlanillaGrid.tsx"
      provides: "Client Component principal — tabla interactiva con estado de filtros"
      exports: ["PlanillaGrid"]
    - path: "components/planilla/ShiftCell.tsx"
      provides: "Celda individual con horario, badge área, indicador manual"
      exports: ["ShiftCell"]
    - path: "components/planilla/MonthNavigator.tsx"
      provides: "Selector de mes anterior/siguiente via router.push"
      exports: ["MonthNavigator"]
    - path: "components/planilla/PlanillaToolbar.tsx"
      provides: "Barra de filtros y botones de acción"
      exports: ["PlanillaToolbar"]
    - path: "lib/actions/shifts.ts"
      provides: "Server Actions para Generar Mes, Limpiar Turnos, Limpiar Novedades"
      exports: ["generateMonth", "clearShifts", "clearAbsences"]
  key_links:
    - from: "app/(protected)/planilla/page.tsx"
      to: "lib/db/shifts.ts + lib/db/employees.ts"
      via: "Promise.all con await en RSC"
      pattern: "Promise\\.all\\(\\[getEmployees"
    - from: "components/planilla/PlanillaGrid.tsx"
      to: "components/planilla/ShiftCell.tsx"
      via: "render de cada celda en el loop de días"
      pattern: "<ShiftCell"
    - from: "components/planilla/PlanillaToolbar.tsx"
      to: "lib/actions/shifts.ts"
      via: "startTransition + Server Action"
      pattern: "startTransition.*generateMonth"
---

<objective>
Construir la planilla principal: RSC shell, tabla cronograma completa con celdas interactivas, filtros y toolbar de acciones.

Purpose: Esta es la vista central del sistema. Sin ella, ninguna funcionalidad de Phase 3 es visible.
Output: La ruta /planilla muestra el cronograma completo del mes actual con filtros funcionales y botones de acción conectados.
</objective>

<execution_context>
@/Users/franciatrasvina/.claude/get-shit-done/workflows/execute-plan.md
@/Users/franciatrasvina/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-planilla-principal/03-01-SUMMARY.md

@types/planilla.ts
@lib/db/shifts.ts
@lib/db/employees.ts
@lib/db/templates.ts
@lib/constants/areas.ts
@lib/utils/format.ts
@app/globals.css
@components/layout/Navbar.tsx
</context>

<interfaces>
<!-- Tipos del Plan 01 — usar directamente -->

```typescript
// types/planilla.ts
export interface MonthYear { mes: number; anio: number }
export interface DayHeader { date: Date; dayNum: number; dayLabel: string; isSunday: boolean; isToday: boolean }
export interface ShiftData { id: number; employeeId: number; date: string; timeSlot: string | null; areaId: number | null; areaName: string | null; areaColor: string | null; isManual: boolean }
export interface AbsenceData { id: number; employeeId: number; startDate: string; endDate: string; type: 'VAC' | 'INC' | 'PER' | 'CAL' | 'DESCANSO' }
export interface EmployeeRow { id: number; name: string; areaIds: number[]; areaNames: string[] }
export interface TemplateOption { id: number; areaId: number; areaName: string; dayOfWeek: number; timeSlot: string; isWildcard: boolean; requiredCount: number }
export interface PlanillaData { employees: EmployeeRow[]; shifts: ShiftData[]; absences: AbsenceData[]; templates: TemplateOption[]; mes: number; anio: number }
```

```typescript
// lib/utils/format.ts
export function formatMonthYear(date: Date): string   // "Marzo 2025"
export function formatDayShort(date: Date): string    // "lun.", "mar."
```

```typescript
// lib/constants/areas.ts
export const AREA_COLORS: Record<string, string>
export const AREA_NAMES: string[]  // ["General", "Valery Camacho", "Buffet", ...]
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: RSC shell de la planilla y acciones de servidor</name>
  <files>app/(protected)/planilla/page.tsx, lib/actions/shifts.ts</files>
  <action>
**app/(protected)/planilla/page.tsx** — Server Component async

Leer `searchParams.mes` y `searchParams.anio` (strings). Si no existen, usar el mes y año actuales. Cargar datos en paralelo con Promise.all. Pasar todo al `PlanillaGrid` Client Component.

```typescript
import { PlanillaGrid } from "@/components/planilla/PlanillaGrid"
import { getEmployeesWithAreas } from "@/lib/db/employees"
import { getShiftsForMonth, getAbsencesForMonth } from "@/lib/db/shifts"
import { getShiftTemplates } from "@/lib/db/templates"

export default async function PlanillaPage({
  searchParams,
}: {
  searchParams: Promise<{ mes?: string; anio?: string }>
}) {
  const params = await searchParams
  const now = new Date()
  const mes = params.mes ? parseInt(params.mes) : now.getMonth() + 1
  const anio = params.anio ? parseInt(params.anio) : now.getFullYear()

  const [employees, shifts, absences, templates] = await Promise.all([
    getEmployeesWithAreas(),
    getShiftsForMonth(mes, anio),
    getAbsencesForMonth(mes, anio),
    getShiftTemplates(),
  ])

  return (
    <PlanillaGrid
      employees={employees}
      shifts={shifts}
      absences={absences}
      templates={templates}
      mes={mes}
      anio={anio}
    />
  )
}
```

**lib/actions/shifts.ts** — Server Actions (archivo separado, `'use server'` al inicio)

Crear 3 Server Actions:

1. `generateMonth(mes, anio)` — Por ahora, llama al servicio de generación si existe (`lib/services/generation.ts`). Si no existe, crear stub que lanza `new Error("Servicio de generación no implementado (Phase 2)")`. Usar `revalidatePath("/planilla")` al final.

2. `clearShifts(mes, anio)` — Elimina todos los shifts del mes vía `prisma.shift.deleteMany` con filtro por rango de fechas. Luego `revalidatePath("/planilla")`.

3. `clearAbsences(mes, anio)` — Elimina todas las ausencias que se solapan con el mes vía `prisma.absence.deleteMany`. Luego `revalidatePath("/planilla")`.

Todas las acciones verifican sesión con `auth.api.getSession({ headers: await headers() })` y lanzan `"No autorizado"` si no hay sesión.
  </action>
  <verify>
    <automated>cd /Users/franciatrasvina/Projects/GestionTurnos && npx tsc --noEmit 2>&1 | grep -E "planilla/page|actions/shifts" | head -20 || echo "OK"</automated>
  </verify>
  <done>La página /planilla compila sin errores de tipo. Las 3 Server Actions existen en lib/actions/shifts.ts con auth guard.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: PlanillaGrid, ShiftCell, MonthNavigator, PlanillaToolbar</name>
  <files>components/planilla/PlanillaGrid.tsx, components/planilla/ShiftCell.tsx, components/planilla/MonthNavigator.tsx, components/planilla/PlanillaToolbar.tsx</files>
  <action>
**Crear el directorio components/planilla/ y los 4 componentes:**

---

**components/planilla/MonthNavigator.tsx** — Client Component

Muestra `"< Marzo 2025 >"` con botones de flecha. Al hacer clic en `<` o `>`, calcula el mes anterior/siguiente y navega a `?mes=N&anio=AAAA` via `router.push`. Usar `date-fns` `addMonths`, `subMonths`. Mostrar el mes actual formateado con `formatMonthYear`. Tailwind: `flex items-center gap-3 text-white font-semibold`.

---

**components/planilla/PlanillaToolbar.tsx** — Client Component

Props: `{ mes: number; anio: number; areaFilter: string[]; novedadFilter: string[]; onAreaFilter: (area: string) => void; onNovedadFilter: (novedad: string) => void; templates: TemplateOption[] }`

**Layout (dos filas):**

Fila 1 — Botones de acción (izquierda a derecha):
- "Generar Mes" (azul/primary) — llama `generateMonth(mes, anio)` con `useTransition`
- "Asignación Manual" (secondary) — navega a `/asignacion-manual?mes=N&anio=AAAA`
- "Excel" (secondary) — placeholder, `alert("Próximamente — Phase 5")`
- "Limpiar Turnos" (rojo/destructive) — `clearShifts(mes, anio)` con confirm dialog
- "Limpiar Novedades" (rojo/destructive) — `clearAbsences(mes, anio)` con confirm dialog

Fila 2 — Filtros:
- Label "Área:" + botones toggle para cada área de `AREA_NAMES`. Cada botón tiene el color del área como background cuando está activo. Al hacer clic, llama `onAreaFilter(areaName)`.
- Label "Novedades:" + botones toggle para: VAC (azul), INC (naranja), PER (verde), CAL (morado). Al hacer clic, llama `onNovedadFilter(tipo)`.

Estilo general: `glass-card p-3 mb-4` usando las variables CSS del proyecto.

---

**components/planilla/ShiftCell.tsx** — Client Component

Props: `{ shift: ShiftData | null; absence: AbsenceData | null; isCurrentDay?: boolean }`

**Lógica de renderizado:**

Si `absence` existe: mostrar el tipo con color semántico:
- VAC → fondo azul `#3b82f6`
- INC → fondo naranja `#f97316`
- PER → fondo verde `#22c55e`
- CAL → fondo morado `#a855f7`
- DESCANSO → fondo gris `#6b7280`

Si `shift` existe y `timeSlot` es null: mostrar "DESC" con fondo gris.

Si `shift` existe y `timeSlot` no es null:
- Mostrar el horario. Si `timeSlot` contiene `|`, mostrar las dos franjas en líneas separadas (split por `|`)
- Mostrar badge del área debajo: fondo = `shift.areaColor`, texto blanco, text-xs, rounded-full, px-1
- Si `shift.isManual === true`, mostrar ✏️ en esquina superior derecha

Si no hay shift ni absence: celda vacía `—`.

Estilos: `min-h-[52px] p-1 text-xs rounded flex flex-col gap-0.5 relative`. Fondo por defecto transparente (la tabla tiene border).

---

**components/planilla/PlanillaGrid.tsx** — Client Component principal (`'use client'`)

Props: `PlanillaData` (todos los campos del tipo)

**Estado interno:**
```typescript
const [areaFilter, setAreaFilter] = useState<string[]>([])
const [novedadFilter, setNovedadFilter] = useState<string[]>([])
```

**Generar encabezados de columna:**
Usar `date-fns` `eachDayOfInterval`, `startOfMonth`, `endOfMonth`, `isSunday`, `isToday`, `getDate`. Para cada día construir `DayHeader`.

**Filtrado de empleados:**
- Si `areaFilter` no está vacío: mostrar solo empleados cuyo `areaNames` intersecta con `areaFilter`.
- Si `novedadFilter` no está vacío: mostrar solo empleados que tienen al menos una ausencia del mes con tipo en `novedadFilter`.

**Lookup de turnos y ausencias:**
Construir dos Maps para lookup O(1):
```typescript
// key: `${employeeId}-${dateISO}`
const shiftMap = new Map<string, ShiftData>()
const absenceMap = new Map<string, AbsenceData>()  // para cada día del rango
```

Para absenceMap: al cargar, expandir cada ausencia por días individuales del rango que caigan dentro del mes.

**Tabla:**
```
<div class="overflow-x-auto">
  <table class="border-collapse text-xs">
    <thead sticky>
      <tr>
        <th sticky-left> ASESOR </th>
        {days.map(day => <th style={day.isSunday ? "color: red" : ""}>
          <div>{day.dayLabel}</div>
          <div class={day.isToday ? "bg-primary rounded-full" : ""}>{day.dayNum}</div>
        </th>)}
      </tr>
    </thead>
    <tbody>
      {filteredEmployees.map(emp => (
        <tr>
          <td sticky-left class="font-medium text-white min-w-[140px] cursor-pointer">
            {emp.name}
          </td>
          {days.map(day => (
            <td class={day.isSunday ? "bg-red-950/20" : ""}>
              <ShiftCell
                shift={shiftMap.get(`${emp.id}-${day.dateISO}`) ?? null}
                absence={absenceMap.get(`${emp.id}-${day.dateISO}`) ?? null}
                isCurrentDay={day.isToday}
              />
            </td>
          ))}
        </tr>
      ))}
    </tbody>
  </table>
</div>
```

**Sticky headers:** La columna "ASESOR" y la primera `<th>` usan `position: sticky; left: 0; z-index: 20; background: var(--card)`. El `<thead>` usa `position: sticky; top: 0; z-index: 10; background: var(--card)`.

Encapsular todo en:
```jsx
<div>
  <div class="flex items-center justify-between mb-4">
    <MonthNavigator mes={mes} anio={anio} />
  </div>
  <PlanillaToolbar ... />
  <div class="glass-card overflow-hidden">
    <div class="overflow-auto max-h-[calc(100vh-220px)]">
      {tabla}
    </div>
  </div>
</div>
```

Nota: El clic en nombre de empleado y el clic en día del encabezado (SIDE-01, SIDE-02) se implementarán en el Plan 03, que agrega state para paneles laterales. Por ahora dejar `onClick={() => {}}` sin funcionalidad.
  </action>
  <verify>
    <automated>cd /Users/franciatrasvina/Projects/GestionTurnos && npx tsc --noEmit 2>&1 | grep -E "components/planilla" | head -30 || echo "OK"</automated>
  </verify>
  <done>Los 4 componentes existen en components/planilla/. La ruta /planilla renderiza el cronograma mensual completo. Los filtros de área y novedad funcionan client-side. El TypeScript compila sin errores.</done>
</task>

</tasks>

<verification>
- Visitar http://localhost:3000/planilla — tabla con filas de empleados y columnas de días del mes actual
- Domingos tienen encabezado en texto rojo
- Día actual tiene número marcado con fondo azul (--primary)
- Los botones de filtro de área cambian de color al activarse
- El navegador de mes muestra "< Nombre Mes Año >"
- `npx tsc --noEmit` pasa sin errores
</verification>

<success_criteria>
La planilla principal es visible y funcional en /planilla. El cronograma muestra empleados x días con los indicadores visuales correctos. Los filtros y navegación de mes funcionan sin recargar la página.
</success_criteria>

<output>
After completion, create `.planning/phases/03-planilla-principal/03-02-SUMMARY.md`
</output>
