---
phase: 03-planilla-principal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - types/planilla.ts
  - lib/db/shifts.ts
  - lib/db/employees.ts
  - lib/db/templates.ts
autonomous: true
requirements:
  - PLAN-01
  - PLAN-06
  - PLAN-07
  - PLAN-08
must_haves:
  truths:
    - "Los tipos TypeScript para empleado, turno, novedad y plantilla están definidos y exportados"
    - "Las queries Prisma para cargar datos de un mes devuelven los tipos correctos"
    - "El compilador TypeScript pasa sin errores en los archivos de tipos y queries"
  artifacts:
    - path: "types/planilla.ts"
      provides: "Contratos TypeScript centrales para toda la planilla"
      exports: ["EmployeeRow", "ShiftData", "AbsenceData", "TemplateOption", "PlanillaData", "DayHeader"]
    - path: "lib/db/shifts.ts"
      provides: "Queries Prisma para turnos y ausencias del mes"
      exports: ["getShiftsForMonth", "getAbsencesForMonth"]
    - path: "lib/db/employees.ts"
      provides: "Query Prisma para empleados con áreas"
      exports: ["getEmployeesWithAreas"]
    - path: "lib/db/templates.ts"
      provides: "Query Prisma para plantillas de turno"
      exports: ["getShiftTemplates"]
  key_links:
    - from: "types/planilla.ts"
      to: "lib/db/shifts.ts"
      via: "return type annotation"
      pattern: "Promise<ShiftData\\[\\]>"
    - from: "lib/db/shifts.ts"
      to: "prisma.shift"
      via: "Prisma include con area"
      pattern: "prisma\\.shift\\.findMany"
---

<objective>
Define los contratos TypeScript y las queries de base de datos que toda la planilla consume.

Purpose: Sin tipos compartidos, los componentes de la planilla se vuelven frágiles. Al definirlos primero, los planes 02-05 reciben interfaces concretas y pueden construir sin explorar el codebase.
Output: 4 archivos (1 de tipos, 3 de queries) listos para ser importados por los componentes de planilla.
</objective>

<execution_context>
@/Users/franciatrasvina/.claude/get-shit-done/workflows/execute-plan.md
@/Users/franciatrasvina/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-planilla-principal/03-01-PLAN.md

@prisma/schema.prisma
@lib/db.ts
@lib/constants/areas.ts
</context>

<interfaces>
<!-- Schema relevante — executor usa esto directamente -->

Del prisma/schema.prisma:
```prisma
model Employee {
  id        Int            @id @default(autoincrement())
  name      String
  areas     EmployeeArea[]
  shifts    Shift[]
  absences  Absence[]
}

model Shift {
  id         Int      @id @default(autoincrement())
  employeeId Int
  date       DateTime @db.Date
  timeSlot   String?         // null = descanso; "7:00-14:00" = simple; "7:00-11:00|11:30-14:30" = partido
  areaId     Int?
  isManual   Boolean  @default(false)
}

model Absence {
  id         Int      @id @default(autoincrement())
  employeeId Int
  startDate  DateTime @db.Date
  endDate    DateTime @db.Date
  type       String   // VAC | INC | PER | CAL | DESCANSO
}

model ShiftTemplate {
  id            Int     @id @default(autoincrement())
  areaId        Int
  dayOfWeek     Int     // 1=Lun ... 7=Dom
  timeSlot      String
  isWildcard    Boolean @default(false)
  requiredCount Int     @default(1)
}
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Definir tipos TypeScript centrales para planilla</name>
  <files>types/planilla.ts</files>
  <action>
Crear `types/planilla.ts` con los tipos que toda la planilla comparte. Exportar estos tipos exactos:

```typescript
// Opciones del selector de mes
export interface MonthYear {
  mes: number   // 1-12
  anio: number  // e.g. 2025
}

// Encabezado de columna del cronograma (un día del mes)
export interface DayHeader {
  date: Date
  dayNum: number      // 1-31
  dayLabel: string    // "lun", "mar" ... (date-fns es locale)
  isSunday: boolean
  isToday: boolean
}

// Turno de un empleado en un día
export interface ShiftData {
  id: number
  employeeId: number
  date: string          // ISO date "2025-03-15"
  timeSlot: string | null  // null = descanso
  areaId: number | null
  areaName: string | null
  areaColor: string | null // hex color from AREA_COLORS
  isManual: boolean
}

// Novedad (ausencia) de un empleado
export interface AbsenceData {
  id: number
  employeeId: number
  startDate: string   // ISO date
  endDate: string     // ISO date
  type: 'VAC' | 'INC' | 'PER' | 'CAL' | 'DESCANSO'
}

// Empleado con sus áreas habilitadas
export interface EmployeeRow {
  id: number
  name: string
  areaIds: number[]
  areaNames: string[]
}

// Plantilla de turno para el modal de asignación
export interface TemplateOption {
  id: number
  areaId: number
  areaName: string
  dayOfWeek: number
  timeSlot: string
  isWildcard: boolean
  requiredCount: number
}

// Prop raíz del PlanillaGrid Client Component
export interface PlanillaData {
  employees: EmployeeRow[]
  shifts: ShiftData[]
  absences: AbsenceData[]
  templates: TemplateOption[]
  mes: number
  anio: number
}
```

No añadir implementaciones — solo tipos. Usar `date-fns` solo en los helpers de query, no en este archivo de tipos. Los campos de fecha en ShiftData/AbsenceData son strings ISO para evitar serialización server→client de objetos Date.
  </action>
  <verify>
    <automated>cd /Users/franciatrasvina/Projects/GestionTurnos && npx tsc --noEmit 2>&1 | grep "types/planilla" | head -20 || echo "OK: sin errores en types/planilla.ts"</automated>
  </verify>
  <done>El archivo types/planilla.ts existe con todos los tipos exportados y el compilador no reporta errores en ese archivo.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Crear capa de queries Prisma para planilla</name>
  <files>lib/db/shifts.ts, lib/db/employees.ts, lib/db/templates.ts</files>
  <action>
Crear `lib/db/` directory con 3 archivos de queries. Importar siempre desde `@/lib/db` (el singleton Prisma) y los tipos desde `@/types/planilla`.

**lib/db/employees.ts**
```typescript
import { prisma } from "@/lib/db"
import type { EmployeeRow } from "@/types/planilla"

export async function getEmployeesWithAreas(): Promise<EmployeeRow[]> {
  const employees = await prisma.employee.findMany({
    include: { areas: { include: { area: true } } },
    orderBy: { name: "asc" },
  })
  return employees.map((e) => ({
    id: e.id,
    name: e.name,
    areaIds: e.areas.map((ea) => ea.area.id),
    areaNames: e.areas.map((ea) => ea.area.name),
  }))
}
```

**lib/db/shifts.ts**
```typescript
import { prisma } from "@/lib/db"
import { startOfMonth, endOfMonth } from "date-fns"
import type { ShiftData, AbsenceData } from "@/types/planilla"
import { AREA_COLORS } from "@/lib/constants/areas"

export async function getShiftsForMonth(mes: number, anio: number): Promise<ShiftData[]> {
  const start = startOfMonth(new Date(anio, mes - 1))
  const end = endOfMonth(new Date(anio, mes - 1))
  const shifts = await prisma.shift.findMany({
    where: { date: { gte: start, lte: end } },
    include: { area: true },
  })
  return shifts.map((s) => ({
    id: s.id,
    employeeId: s.employeeId,
    date: s.date.toISOString().split("T")[0],
    timeSlot: s.timeSlot,
    areaId: s.areaId,
    areaName: s.area?.name ?? null,
    areaColor: s.area ? (AREA_COLORS[s.area.name] ?? null) : null,
    isManual: s.isManual,
  }))
}

export async function getAbsencesForMonth(mes: number, anio: number): Promise<AbsenceData[]> {
  const start = startOfMonth(new Date(anio, mes - 1))
  const end = endOfMonth(new Date(anio, mes - 1))
  const absences = await prisma.absence.findMany({
    where: {
      OR: [
        { startDate: { lte: end }, endDate: { gte: start } },
      ],
    },
  })
  return absences.map((a) => ({
    id: a.id,
    employeeId: a.employeeId,
    startDate: a.startDate.toISOString().split("T")[0],
    endDate: a.endDate.toISOString().split("T")[0],
    type: a.type as AbsenceData["type"],
  }))
}
```

**lib/db/templates.ts**
```typescript
import { prisma } from "@/lib/db"
import type { TemplateOption } from "@/types/planilla"

export async function getShiftTemplates(): Promise<TemplateOption[]> {
  const templates = await prisma.shiftTemplate.findMany({
    include: { area: true },
    orderBy: [{ area: { name: "asc" } }, { dayOfWeek: "asc" }],
  })
  return templates.map((t) => ({
    id: t.id,
    areaId: t.areaId,
    areaName: t.area.name,
    dayOfWeek: t.dayOfWeek,
    timeSlot: t.timeSlot,
    isWildcard: t.isWildcard,
    requiredCount: t.requiredCount,
  }))
}
```

Verificar que no haya conflicto de nombres con el directorio `lib/db.ts` existente — los archivos van en `lib/db/` (subdirectorio), no en `lib/db.ts`.
  </action>
  <verify>
    <automated>cd /Users/franciatrasvina/Projects/GestionTurnos && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Los 3 archivos de query existen en lib/db/, el compilador no reporta errores de tipos, y cada función exporta el tipo correcto de types/planilla.ts.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` pasa sin errores en los 4 archivos nuevos
- `types/planilla.ts` exporta: MonthYear, DayHeader, ShiftData, AbsenceData, EmployeeRow, TemplateOption, PlanillaData
- `lib/db/shifts.ts` exporta: getShiftsForMonth, getAbsencesForMonth
- `lib/db/employees.ts` exporta: getEmployeesWithAreas
- `lib/db/templates.ts` exporta: getShiftTemplates
</verification>

<success_criteria>
Los tipos y queries están listos para ser consumidos por los componentes de planilla. Cualquier plan posterior puede importar desde `@/types/planilla` y `@/lib/db/*` sin explorar el codebase.
</success_criteria>

<output>
After completion, create `.planning/phases/03-planilla-principal/03-01-SUMMARY.md`
</output>
