---
phase: 03-planilla-principal
plan: 04
type: execute
wave: 3
depends_on:
  - "03-02"
files_modified:
  - app/(protected)/asignacion-manual/page.tsx
  - components/planilla/AsignacionGrid.tsx
  - components/planilla/AsignacionModal.tsx
  - lib/actions/shifts.ts
autonomous: true
requirements:
  - MAN-01
  - MAN-02
  - MAN-03
  - MAN-04
  - MAN-05
  - MAN-06
  - MAN-07
must_haves:
  truths:
    - "La ruta /asignacion-manual muestra la misma estructura de tabla que la planilla pero editable"
    - "La columna de nombres y la fila de encabezado son sticky en ambas direcciones durante el scroll"
    - "Hacer clic en una celda abre un modal lateral con plantillas de turno agrupadas por área"
    - "El modal incluye opciones de novedades (VAC, INC, PER, CAL, DESCANSO)"
    - "Guardar en el modal actualiza la celda via Server Action sin recargar la página"
    - "Las celdas con turno manual muestran ✏️ como indicador visual"
  artifacts:
    - path: "app/(protected)/asignacion-manual/page.tsx"
      provides: "RSC shell de la vista de asignación manual"
    - path: "components/planilla/AsignacionGrid.tsx"
      provides: "Tabla editable con sticky headers, misma estructura que PlanillaGrid"
      exports: ["AsignacionGrid"]
    - path: "components/planilla/AsignacionModal.tsx"
      provides: "Modal lateral slide-over para seleccionar turno o novedad"
      exports: ["AsignacionModal"]
  key_links:
    - from: "components/planilla/AsignacionModal.tsx"
      to: "lib/actions/shifts.ts"
      via: "startTransition + assignShift Server Action"
      pattern: "assignShift.*employeeId.*date.*templateId"
    - from: "components/planilla/AsignacionGrid.tsx"
      to: "components/planilla/AsignacionModal.tsx"
      via: "selectedCell state + JSX condicional"
      pattern: "selectedCell.*AsignacionModal"
---

<objective>
Construir la vista de asignación manual: tabla editable con sticky headers, modal lateral de plantillas, y guardado AJAX via Server Action.

Purpose: La generación automática necesita correcciones manuales. Esta vista es el mecanismo de corrección celda por celda para toda la planilla.
Output: La ruta /asignacion-manual es funcional con scroll sticky y modal de asignación operativo.
</objective>

<execution_context>
@/Users/franciatrasvina/.claude/get-shit-done/workflows/execute-plan.md
@/Users/franciatrasvina/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-planilla-principal/03-02-SUMMARY.md

@types/planilla.ts
@lib/db/shifts.ts
@lib/db/employees.ts
@lib/db/templates.ts
@lib/actions/shifts.ts
@app/globals.css
</context>

<interfaces>
<!-- Tipos del Plan 01 -->
```typescript
interface ShiftData { id: number; employeeId: number; date: string; timeSlot: string|null; areaId: number|null; areaName: string|null; areaColor: string|null; isManual: boolean }
interface EmployeeRow { id: number; name: string; areaIds: number[]; areaNames: string[] }
interface TemplateOption { id: number; areaId: number; areaName: string; dayOfWeek: number; timeSlot: string; isWildcard: boolean; requiredCount: number }
interface AbsenceData { id: number; employeeId: number; startDate: string; endDate: string; type: 'VAC'|'INC'|'PER'|'CAL'|'DESCANSO' }
```

<!-- Server Actions del Plan 02 que hay que AMPLIAR -->
```typescript
// lib/actions/shifts.ts (ya existe — agregar estas firmas)
export async function assignShift(
  employeeId: number,
  date: string,          // ISO "2025-03-15"
  templateId: number | null,  // null = descanso o novedad
  absenceType: string | null  // null = turno, "VAC"|"INC"|"PER"|"CAL"|"DESCANSO" = novedad
): Promise<void>
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Server Action assignShift y RSC shell de asignación manual</name>
  <files>lib/actions/shifts.ts, app/(protected)/asignacion-manual/page.tsx</files>
  <action>
**lib/actions/shifts.ts** — Agregar la Server Action `assignShift`

Agregar al archivo existente (sin reemplazar las acciones ya creadas en Plan 02):

```typescript
export async function assignShift(
  employeeId: number,
  date: string,          // ISO "2025-03-15"
  templateId: number | null,
  absenceType: string | null
): Promise<void> {
  const session = await auth.api.getSession({ headers: await headers() })
  if (!session) throw new Error("No autorizado")

  const dateObj = new Date(date + "T00:00:00")

  if (absenceType) {
    // Es una novedad: crear absence de un día
    await prisma.absence.create({
      data: {
        employeeId,
        startDate: dateObj,
        endDate: dateObj,
        type: absenceType,
      },
    })
    // Si había un shift ese día, eliminarlo
    await prisma.shift.deleteMany({ where: { employeeId, date: dateObj } })
  } else if (templateId === null) {
    // Descanso: shift con timeSlot null
    await prisma.shift.upsert({
      where: { employeeId_date: { employeeId, date: dateObj } },
      create: { employeeId, date: dateObj, timeSlot: null, isManual: true },
      update: { timeSlot: null, areaId: null, isManual: true },
    })
    // Si había una ausencia ese día, eliminarla
    await prisma.absence.deleteMany({
      where: { employeeId, startDate: dateObj, endDate: dateObj },
    })
  } else {
    // Turno con plantilla: buscar la plantilla, upsert el shift
    const template = await prisma.shiftTemplate.findUnique({
      where: { id: templateId },
      include: { area: true },
    })
    if (!template) throw new Error("Plantilla no encontrada")
    await prisma.shift.upsert({
      where: { employeeId_date: { employeeId, date: dateObj } },
      create: { employeeId, date: dateObj, timeSlot: template.timeSlot, areaId: template.areaId, isManual: true },
      update: { timeSlot: template.timeSlot, areaId: template.areaId, isManual: true },
    })
    await prisma.absence.deleteMany({
      where: { employeeId, startDate: dateObj, endDate: dateObj },
    })
  }

  revalidatePath("/planilla")
  revalidatePath("/asignacion-manual")
}
```

---

**app/(protected)/asignacion-manual/page.tsx** — RSC shell

Idéntico al shell de planilla: leer `?mes=N&anio=AAAA` de searchParams, cargar datos con Promise.all, pasar al `AsignacionGrid`. El mes/año por defecto es el actual.

```typescript
import { AsignacionGrid } from "@/components/planilla/AsignacionGrid"
import { getEmployeesWithAreas } from "@/lib/db/employees"
import { getShiftsForMonth, getAbsencesForMonth } from "@/lib/db/shifts"
import { getShiftTemplates } from "@/lib/db/templates"

export default async function AsignacionManualPage({ searchParams }: { searchParams: Promise<{ mes?: string; anio?: string }> }) {
  const params = await searchParams
  const now = new Date()
  const mes = params.mes ? parseInt(params.mes) : now.getMonth() + 1
  const anio = params.anio ? parseInt(params.anio) : now.getFullYear()

  const [employees, shifts, absences, templates] = await Promise.all([
    getEmployeesWithAreas(),
    getShiftsForMonth(mes, anio),
    getAbsencesForMonth(mes, anio),
    getShiftTemplates(),
  ])

  return <AsignacionGrid employees={employees} shifts={shifts} absences={absences} templates={templates} mes={mes} anio={anio} />
}
```
  </action>
  <verify>
    <automated>cd /Users/franciatrasvina/Projects/GestionTurnos && npx tsc --noEmit 2>&1 | grep -E "asignacion-manual|actions/shifts" | head -20 || echo "OK"</automated>
  </verify>
  <done>La Server Action assignShift existe y cubre los 3 casos: novedad, descanso, turno con plantilla. La página RSC compila sin errores.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: AsignacionGrid con sticky headers y AsignacionModal</name>
  <files>components/planilla/AsignacionGrid.tsx, components/planilla/AsignacionModal.tsx</files>
  <action>
**components/planilla/AsignacionModal.tsx** — Client Component (`'use client'`)

Props:
```typescript
interface AsignacionModalProps {
  employeeId: number
  employeeName: string
  date: string          // ISO "2025-03-15"
  dayLabel: string      // "lunes 15 de marzo"
  templates: TemplateOption[]
  onClose: () => void
  onSaved: () => void
}
```

**Layout:** Slide-over fijo a la derecha. Ancho `w-80`. Fondo `glass-card` con backdrop. Overlay oscuro detrás (`bg-black/40 fixed inset-0`).

**Contenido:**
1. Cabecera: `{employeeName} — {dayLabel}` + botón X
2. Sección "Novedades" — 5 botones en grid 2col con colores semánticos:
   - VAC (azul) — VACACIONES
   - INC (naranja) — INCAPACIDAD
   - PER (verde) — PERMISO
   - CAL (morado) — CALAMIDAD
   - DESCANSO (gris)
3. Sección "Turnos" — plantillas agrupadas por área. Para cada área, un subgrupo con nombre de área (badge de color) y las plantillas del área filtradas por `dayOfWeek === date.getDay() (ajustar: date-fns getDay() devuelve 0=Dom, pero dayOfWeek en DB es 1=Lun...7=Dom)`.

Al hacer clic en cualquier opción:
```typescript
const [pending, startTransition] = useTransition()
function handleSelect(templateId: number | null, absenceType: string | null) {
  startTransition(async () => {
    await assignShift(employeeId, date, templateId, absenceType)
    onSaved()
    onClose()
  })
}
```

Botón "Quitar turno" al fondo (eliminar el shift/ausencia del día via `assignShift` con `templateId=null, absenceType=null` — esto crea un descanso; si se quiere celda vacía, agregar un caso extra en la action). Para este caso, mostrar "DESCANSO" en la celda.

---

**components/planilla/AsignacionGrid.tsx** — Client Component (`'use client'`)

Props: `PlanillaData` (mismo tipo que PlanillaGrid).

Estado interno:
```typescript
const [selectedCell, setSelectedCell] = useState<{ employeeId: number; date: string; employeeName: string; dayLabel: string } | null>(null)
const [localShifts, setLocalShifts] = useState<ShiftData[]>(shifts)
```

Usar `localShifts` en vez de `shifts` para mostrar el estado optimista antes de que el RSC revalide.

**Tabla con sticky headers:**

Los sticky headers son el requisito MAN-02/MAN-03. Implementar con CSS puro (no overflow en `<table>`; en cambio, envolver en un `div` con `overflow-auto` y altura fija):

```html
<div class="overflow-auto" style="max-height: calc(100vh - 180px)">
  <table style="border-collapse: collapse">
    <thead>
      <tr>
        <!-- Celda esquina — sticky en X e Y -->
        <th style="position: sticky; left: 0; top: 0; z-index: 30; background: var(--card)">
          ASESOR
        </th>
        <!-- Encabezados de día — sticky en Y -->
        {days.map(day => (
          <th style="position: sticky; top: 0; z-index: 20; background: var(--card); color: day.isSunday ? #ef4444 : inherit">
            <div>{day.dayLabel}</div>
            <div>{day.dayNum}</div>
          </th>
        ))}
      </tr>
    </thead>
    <tbody>
      {employees.map(emp => (
        <tr>
          <!-- Nombre — sticky en X -->
          <td style="position: sticky; left: 0; z-index: 10; background: var(--card); min-width: 140px; cursor: default">
            {emp.name}
          </td>
          <!-- Celdas editables -->
          {days.map(day => (
            <td
              class="cursor-pointer hover:bg-white/10 border border-white/5"
              onClick={() => setSelectedCell({
                employeeId: emp.id,
                employeeName: emp.name,
                date: day.dateISO,
                dayLabel: format(day.date, "EEEE d 'de' MMMM", { locale: es }),
              })}
            >
              <ShiftCell
                shift={shiftMap.get(`${emp.id}-${day.dateISO}`) ?? null}
                absence={absenceMap.get(`${emp.id}-${day.dateISO}`) ?? null}
              />
            </td>
          ))}
        </tr>
      ))}
    </tbody>
  </table>
</div>
```

Reutilizar `ShiftCell` del Plan 02 (`@/components/planilla/ShiftCell`).

Agregar encima de la tabla el `MonthNavigator` del Plan 02 para poder cambiar de mes en esta vista también.

Renderizar `AsignacionModal` condicionalmente cuando `selectedCell !== null`:
```jsx
{selectedCell && (
  <AsignacionModal
    employeeId={selectedCell.employeeId}
    employeeName={selectedCell.employeeName}
    date={selectedCell.date}
    dayLabel={selectedCell.dayLabel}
    templates={templates}
    onClose={() => setSelectedCell(null)}
    onSaved={() => {
      setSelectedCell(null)
      // localShifts se refresca con la revalidatePath del server
    }}
  />
)}
```
  </action>
  <verify>
    <automated>cd /Users/franciatrasvina/Projects/GestionTurnos && npx tsc --noEmit 2>&1 | grep -E "AsignacionGrid|AsignacionModal" | head -30 || echo "OK"</automated>
  </verify>
  <done>La vista /asignacion-manual muestra la tabla con sticky headers funcionales. Hacer clic en una celda abre el modal lateral. Seleccionar una plantilla o novedad llama a assignShift y cierra el modal. TypeScript compila sin errores.</done>
</task>

</tasks>

<verification>
- Visitar http://localhost:3000/asignacion-manual — tabla idéntica a /planilla pero con celdas clickeables
- Hacer scroll horizontal y vertical — "ASESOR" permanece en la esquina superior izquierda
- Clic en cualquier celda — modal slide-over aparece a la derecha con plantillas y novedades
- Seleccionar una plantilla — modal se cierra, celda se actualiza (tras revalidación RSC)
- `npx tsc --noEmit` pasa sin errores
</verification>

<success_criteria>
La asignación manual es funcional de extremo a extremo: sticky headers, modal lateral con plantillas y novedades, y guardado AJAX via Server Action.
</success_criteria>

<output>
After completion, create `.planning/phases/03-planilla-principal/03-04-SUMMARY.md`
</output>
