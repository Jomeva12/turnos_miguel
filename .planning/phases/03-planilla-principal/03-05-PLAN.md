---
phase: 03-planilla-principal
plan: 05
type: execute
wave: 4
depends_on:
  - "03-03"
  - "03-04"
files_modified:
  - app/(protected)/editor/[empleadoId]/page.tsx
  - components/planilla/EditorIndividual.tsx
  - components/planilla/PlanillaGrid.tsx
autonomous: true
requirements:
  - EDIT-01
  - EDIT-02
  - EDIT-03
must_haves:
  truths:
    - "La ruta /editor/[empleadoId] muestra el grid de días del mes para ese empleado específico"
    - "Cada día muestra el turno actual (horario + área) o la novedad vigente"
    - "Hacer clic en cualquier día abre el mismo AsignacionModal del plan anterior"
    - "Guardar un turno via modal actualiza la celda del día sin recargar la página"
    - "El botón Editar Turnos del panel de perfil (Plan 03) navega a esta ruta"
  artifacts:
    - path: "app/(protected)/editor/[empleadoId]/page.tsx"
      provides: "RSC shell para el editor individual de un empleado"
    - path: "components/planilla/EditorIndividual.tsx"
      provides: "Grid de días del mes para un empleado, con modal de asignación"
      exports: ["EditorIndividual"]
  key_links:
    - from: "components/planilla/PlanillaGrid.tsx"
      to: "app/(protected)/editor/[empleadoId]/page.tsx"
      via: "router.push en onEditarTurnos del EmpleadoPerfil"
      pattern: "router\\.push.*editor.*empleadoId"
    - from: "components/planilla/EditorIndividual.tsx"
      to: "components/planilla/AsignacionModal.tsx"
      via: "selectedDay state + renderizado condicional"
      pattern: "selectedDay.*AsignacionModal"
---

<objective>
Construir el editor individual: grid de días del mes para un empleado, con el mismo AsignacionModal reutilizado del Plan 04.

Purpose: La admin necesita revisar y corregir el horario completo de un empleado específico. El editor individual concentra todos los días en una vista compacta sin tener que buscar al empleado en la planilla completa.
Output: La ruta /editor/[empleadoId] es funcional, reutiliza AsignacionModal, y el botón "Editar Turnos" del perfil navega a esta ruta.
</objective>

<execution_context>
@/Users/franciatrasvina/.claude/get-shit-done/workflows/execute-plan.md
@/Users/franciatrasvina/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-planilla-principal/03-03-SUMMARY.md
@.planning/phases/03-planilla-principal/03-04-SUMMARY.md

@types/planilla.ts
@lib/db/shifts.ts
@lib/db/employees.ts
@lib/db/templates.ts
@components/planilla/AsignacionModal.tsx
@components/planilla/EmpleadoPerfil.tsx
@components/planilla/PlanillaGrid.tsx
</context>

<interfaces>
<!-- Tipos del Plan 01 — reutilizar directamente -->
```typescript
interface ShiftData { id: number; employeeId: number; date: string; timeSlot: string|null; areaId: number|null; areaName: string|null; areaColor: string|null; isManual: boolean }
interface AbsenceData { id: number; employeeId: number; startDate: string; endDate: string; type: 'VAC'|'INC'|'PER'|'CAL'|'DESCANSO' }
interface EmployeeRow { id: number; name: string; areaIds: number[]; areaNames: string[] }
interface TemplateOption { id: number; areaId: number; areaName: string; dayOfWeek: number; timeSlot: string; isWildcard: boolean; requiredCount: number }
```

<!-- AsignacionModal ya existe — reutilizar sin modificar -->
```typescript
// components/planilla/AsignacionModal.tsx
interface AsignacionModalProps {
  employeeId: number
  employeeName: string
  date: string
  dayLabel: string
  templates: TemplateOption[]
  onClose: () => void
  onSaved: () => void
}
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: RSC shell del editor individual y componente EditorIndividual</name>
  <files>app/(protected)/editor/[empleadoId]/page.tsx, components/planilla/EditorIndividual.tsx</files>
  <action>
**app/(protected)/editor/[empleadoId]/page.tsx** — RSC shell

Leer `params.empleadoId` (string, convertir a Int) y `searchParams.mes`/`searchParams.anio`. Si el empleado no existe, hacer `notFound()` (importar de `next/navigation`).

```typescript
import { notFound } from "next/navigation"
import { prisma } from "@/lib/db"
import { EditorIndividual } from "@/components/planilla/EditorIndividual"
import { getShiftsForMonth, getAbsencesForMonth } from "@/lib/db/shifts"
import { getShiftTemplates } from "@/lib/db/templates"
import type { EmployeeRow } from "@/types/planilla"

export default async function EditorPage({
  params,
  searchParams,
}: {
  params: Promise<{ empleadoId: string }>
  searchParams: Promise<{ mes?: string; anio?: string }>
}) {
  const { empleadoId } = await params
  const sp = await searchParams
  const id = parseInt(empleadoId)

  const now = new Date()
  const mes = sp.mes ? parseInt(sp.mes) : now.getMonth() + 1
  const anio = sp.anio ? parseInt(sp.anio) : now.getFullYear()

  const [employeeRaw, shifts, absences, templates] = await Promise.all([
    prisma.employee.findUnique({
      where: { id },
      include: { areas: { include: { area: true } } },
    }),
    getShiftsForMonth(mes, anio),
    getAbsencesForMonth(mes, anio),
    getShiftTemplates(),
  ])

  if (!employeeRaw) notFound()

  const employee: EmployeeRow = {
    id: employeeRaw.id,
    name: employeeRaw.name,
    areaIds: employeeRaw.areas.map((ea) => ea.area.id),
    areaNames: employeeRaw.areas.map((ea) => ea.area.name),
  }

  // Filtrar shifts/absences solo del empleado
  const empShifts = shifts.filter((s) => s.employeeId === id)
  const empAbsences = absences.filter((a) => a.employeeId === id)

  return (
    <EditorIndividual
      employee={employee}
      shifts={empShifts}
      absences={empAbsences}
      templates={templates}
      mes={mes}
      anio={anio}
    />
  )
}
```

---

**components/planilla/EditorIndividual.tsx** — Client Component (`'use client'`)

Props:
```typescript
interface EditorIndividualProps {
  employee: EmployeeRow
  shifts: ShiftData[]
  absences: AbsenceData[]
  templates: TemplateOption[]
  mes: number
  anio: number
}
```

Estado interno:
```typescript
const [selectedDay, setSelectedDay] = useState<{ date: string; dayLabel: string } | null>(null)
```

**Layout:**

Encabezado: `← Volver a planilla` (link a `/planilla?mes=N&anio=AAAA`) + nombre del empleado en título grande + `MonthNavigator` (reutilizar del Plan 02).

Grid de días: usar `eachDayOfInterval(startOfMonth, endOfMonth)` para generar todos los días del mes.

Mostrar los días en una cuadrícula `grid-cols-7` (7 columnas = una semana). Cada día es una tarjeta clickeable.

**Cada tarjeta de día:**
```jsx
<div
  key={dateISO}
  onClick={() => setSelectedDay({
    date: dateISO,
    dayLabel: format(day, "EEEE d 'de' MMMM", { locale: es })
  })}
  className="glass p-2 rounded cursor-pointer hover:bg-white/10 min-h-[70px] flex flex-col gap-1"
  style={{ borderColor: day.isSunday ? '#ef4444' : 'var(--glass-border)' }}
>
  {/* Número del día */}
  <span className={`text-xs font-bold ${day.isSunday ? 'text-red-400' : 'text-white/60'} ${day.isToday ? 'bg-primary rounded-full px-1' : ''}`}>
    {format(day, "d")}
  </span>
  {/* Estado del turno */}
  <ShiftCell
    shift={shiftMap.get(dateISO) ?? null}
    absence={absenceMap.get(dateISO) ?? null}
  />
</div>
```

Reutilizar `ShiftCell` importado de `@/components/planilla/ShiftCell`.

**Modal de asignación** — renderizar cuando `selectedDay !== null`:
```jsx
{selectedDay && (
  <AsignacionModal
    employeeId={employee.id}
    employeeName={employee.name}
    date={selectedDay.date}
    dayLabel={selectedDay.dayLabel}
    templates={templates}
    onClose={() => setSelectedDay(null)}
    onSaved={() => setSelectedDay(null)}
  />
)}
```

Reutilizar `AsignacionModal` importado de `@/components/planilla/AsignacionModal` — sin modificar ese componente.
  </action>
  <verify>
    <automated>cd /Users/franciatrasvina/Projects/GestionTurnos && npx tsc --noEmit 2>&1 | grep -E "editor|EditorIndividual" | head -20 || echo "OK"</automated>
  </verify>
  <done>La ruta /editor/[empleadoId] existe y compila. El grid de días se renderiza. El modal de asignación se abre al hacer clic en un día.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Conectar botón Editar Turnos desde panel de perfil</name>
  <files>components/planilla/PlanillaGrid.tsx</files>
  <action>
Modificar la función `onEditarTurnos` en `PlanillaGrid.tsx` para navegar a la ruta del editor individual.

Localizar el `console.log("Editar turnos empleado:", id)` que se dejó como placeholder en el Plan 03.

Reemplazarlo con:
```typescript
import { useRouter } from "next/navigation"

// Dentro del componente PlanillaGrid, agregar:
const router = useRouter()

// En el onEditarTurnos del EmpleadoPerfil:
onEditarTurnos={(id) => {
  router.push(`/editor/${id}?mes=${mes}&anio=${anio}`)
}}
```

Agregar la importación de `useRouter` de `next/navigation` si no existe. Verificar que el archivo ya importa `useRouter` del Plan 02 — si no, agregarlo.

Este es el único cambio en PlanillaGrid.tsx en este plan — no tocar nada más.
  </action>
  <verify>
    <automated>cd /Users/franciatrasvina/Projects/GestionTurnos && npx tsc --noEmit 2>&1 | grep "PlanillaGrid" | head -10 || echo "OK"</automated>
  </verify>
  <done>Al hacer clic en "Editar Turnos" en el panel de perfil de un empleado, la app navega a /editor/[id]?mes=N&anio=AAAA. TypeScript compila sin errores.</done>
</task>

</tasks>

<verification>
- Abrir /planilla → clic en nombre de empleado → panel derecho aparece
- Clic en "Editar Turnos" → navega a /editor/[id]
- El editor muestra el grid de 28-31 días del mes en formato cuadrícula 7 columnas
- Domingos tienen borde rojo
- Clic en cualquier día → modal de asignación se abre con las plantillas del empleado
- Guardar un turno → modal se cierra, celda actualizada en próxima carga
- Volver a /planilla → la celda del empleado y día muestra el turno guardado con ✏️
- `npx tsc --noEmit` pasa sin errores
</verification>

<success_criteria>
Los 23 requisitos de Phase 3 están implementados: planilla principal, filtros, paneles laterales, asignación manual con sticky headers y modal, y editor individual. El flujo completo admin → planilla → editar empleado funciona de extremo a extremo.
</success_criteria>

<output>
After completion, create `.planning/phases/03-planilla-principal/03-05-SUMMARY.md`
</output>
