---
phase: 03-planilla-principal
plan: 03
type: execute
wave: 3
depends_on:
  - "03-02"
files_modified:
  - components/planilla/PlanillaGrid.tsx
  - components/planilla/CoberturaSidebar.tsx
  - components/planilla/EmpleadoPerfil.tsx
autonomous: true
requirements:
  - SIDE-01
  - SIDE-02
must_haves:
  truths:
    - "Al hacer clic en el número de un día del encabezado, se abre el panel izquierdo con resumen de cobertura de ese día"
    - "El panel de cobertura muestra total trabajando, libres, en vacaciones, y desglose por área"
    - "Al hacer clic en el nombre de un empleado, se abre el panel derecho con su perfil del mes"
    - "El panel de perfil muestra nombre, total turnos/descansos/novedades del mes, y botón de editar turnos"
    - "Solo puede estar abierto un panel a la vez; hacer clic en otro día o empleado reemplaza el panel"
  artifacts:
    - path: "components/planilla/CoberturaSidebar.tsx"
      provides: "Panel lateral de cobertura del día seleccionado"
      exports: ["CoberturaSidebar"]
    - path: "components/planilla/EmpleadoPerfil.tsx"
      provides: "Panel lateral de perfil del empleado seleccionado"
      exports: ["EmpleadoPerfil"]
  key_links:
    - from: "components/planilla/PlanillaGrid.tsx"
      to: "components/planilla/CoberturaSidebar.tsx"
      via: "selectedDay state + condicional JSX"
      pattern: "selectedDay.*CoberturaSidebar"
    - from: "components/planilla/PlanillaGrid.tsx"
      to: "components/planilla/EmpleadoPerfil.tsx"
      via: "selectedEmployee state + condicional JSX"
      pattern: "selectedEmployee.*EmpleadoPerfil"
---

<objective>
Agregar los paneles laterales de cobertura por día y perfil de empleado a la planilla existente.

Purpose: La planilla sin paneles laterales obliga a la admin a contar celdas manualmente. Los paneles dan acceso inmediato a la información de cobertura y del empleado sin salir de la planilla.
Output: Dos paneles laterales que se abren al hacer clic en un día o en el nombre de un empleado dentro de PlanillaGrid.
</objective>

<execution_context>
@/Users/franciatrasvina/.claude/get-shit-done/workflows/execute-plan.md
@/Users/franciatrasvina/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-planilla-principal/03-02-SUMMARY.md

@types/planilla.ts
@components/planilla/PlanillaGrid.tsx
@app/globals.css
</context>

<interfaces>
<!-- Tipos disponibles del Plan 01 -->
```typescript
interface ShiftData { id: number; employeeId: number; date: string; timeSlot: string|null; areaId: number|null; areaName: string|null; areaColor: string|null; isManual: boolean }
interface AbsenceData { id: number; employeeId: number; startDate: string; endDate: string; type: 'VAC'|'INC'|'PER'|'CAL'|'DESCANSO' }
interface EmployeeRow { id: number; name: string; areaIds: number[]; areaNames: string[] }
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Componentes de paneles laterales</name>
  <files>components/planilla/CoberturaSidebar.tsx, components/planilla/EmpleadoPerfil.tsx</files>
  <action>
**components/planilla/CoberturaSidebar.tsx** — Client Component

Props:
```typescript
interface CoberturaSidebarProps {
  day: Date | null
  shifts: ShiftData[]
  absences: AbsenceData[]
  employees: EmployeeRow[]
  onClose: () => void
}
```

Si `day` es null, retornar null.

**Calcular para ese día (fecha como ISO string):**
- `trabajando`: empleados que tienen un shift con timeSlot no null (no son descanso)
- `descanso`: empleados con shift donde timeSlot === null
- `vacaciones`: empleados con ausencia de tipo VAC que cubre ese día
- `total`: employees.length

**Desglose por área:** Para cada empleado trabajando, agrupar por `shift.areaName`. Contar cuántos por área. Mostrar con el badge de color del área.

**UI:** Panel fijo a la izquierda de la tabla. Ancho `w-64`. Fondo `glass-card`. Cabecera con la fecha formateada en español (ej. "Lunes 15 de Marzo") con botón X para cerrar. Secciones:
- "Resumen del día": 4 rows con íconos (trabajando, descanso, vacaciones, total)
- "Por área": lista con badge de color + nombre de área + número

Usar `date-fns` `format` con locale `es` para formatear la fecha del encabezado.

---

**components/planilla/EmpleadoPerfil.tsx** — Client Component

Props:
```typescript
interface EmpleadoPerfilProps {
  employee: EmployeeRow | null
  shifts: ShiftData[]
  absences: AbsenceData[]
  mes: number
  anio: number
  onClose: () => void
  onEditarTurnos: (employeeId: number) => void
}
```

Si `employee` es null, retornar null.

**Calcular para ese empleado en el mes:**
- `totalTrabajando`: shifts con timeSlot no null
- `totalDescansos`: shifts con timeSlot === null
- `totalNovedades`: absences (contar entradas, no días)
- `areasEsteMe`: Set de areaNames de sus shifts del mes

**UI:** Panel fijo a la derecha de la tabla. Ancho `w-64`. Fondo `glass-card`. Cabecera con nombre del empleado + botón X. Secciones:
- "Áreas habilitadas": badges con colores
- "Este mes": working / descansos / novedades
- Botón "Editar Turnos" (primary) que llama `onEditarTurnos(employee.id)`

**Nota:** `onEditarTurnos` navegará a la vista de editor individual — se conecta en Plan 05.
  </action>
  <verify>
    <automated>cd /Users/franciatrasvina/Projects/GestionTurnos && npx tsc --noEmit 2>&1 | grep -E "CoberturaSidebar|EmpleadoPerfil" | head -20 || echo "OK"</automated>
  </verify>
  <done>Ambos componentes existen, sus props coinciden con los tipos de types/planilla.ts, el TypeScript compila sin errores.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Conectar paneles laterales en PlanillaGrid</name>
  <files>components/planilla/PlanillaGrid.tsx</files>
  <action>
Modificar `PlanillaGrid.tsx` para agregar el estado y los handlers de los paneles. NO reescribir el componente — solo agregar las secciones indicadas.

**Agregar estado:**
```typescript
const [selectedDay, setSelectedDay] = useState<Date | null>(null)
const [selectedEmployee, setSelectedEmployee] = useState<EmployeeRow | null>(null)
```

**Regla de exclusividad:** Al abrir un panel, cerrar el otro:
```typescript
function handleDayClick(date: Date) {
  setSelectedDay(date)
  setSelectedEmployee(null)
}
function handleEmployeeClick(emp: EmployeeRow) {
  setSelectedEmployee(emp)
  setSelectedDay(null)
}
```

**Conectar al JSX existente:**
1. En los `<th>` de días del encabezado: agregar `onClick={() => handleDayClick(day.date)}` con `cursor-pointer hover:bg-white/5`.
2. En los `<td>` de nombre de empleado: agregar `onClick={() => handleEmployeeClick(emp)}`.

**Layout con paneles:** Envolver la tabla existente en un grid de 3 columnas:
```jsx
<div className="flex gap-4">
  {/* Panel izquierdo */}
  <CoberturaSidebar
    day={selectedDay}
    shifts={shifts}
    absences={absences}
    employees={employees}
    onClose={() => setSelectedDay(null)}
  />

  {/* Tabla principal — flex-1 */}
  <div className="flex-1 min-w-0">
    {/* ...tabla existente... */}
  </div>

  {/* Panel derecho */}
  <EmpleadoPerfil
    employee={selectedEmployee}
    shifts={shifts}
    absences={absences}
    mes={mes}
    anio={anio}
    onClose={() => setSelectedEmployee(null)}
    onEditarTurnos={(id) => {
      // Conectar en Plan 05
      console.log("Editar turnos empleado:", id)
    }}
  />
</div>
```

Cuando ambos paneles están cerrados (null), la tabla ocupa el ancho completo porque los componentes retornan null. No necesita lógica condicional adicional.

Importar `CoberturaSidebar` y `EmpleadoPerfil` al inicio del archivo.
  </action>
  <verify>
    <automated>cd /Users/franciatrasvina/Projects/GestionTurnos && npx tsc --noEmit 2>&1 | grep "PlanillaGrid" | head -20 || echo "OK"</automated>
  </verify>
  <done>Hacer clic en un día del encabezado abre el panel de cobertura a la izquierda. Hacer clic en un nombre de empleado abre el perfil a la derecha. Ambos paneles no pueden estar abiertos simultáneamente.</done>
</task>

</tasks>

<verification>
- Clic en encabezado de día → panel izquierdo aparece con fecha y resumen de cobertura
- Clic en nombre de empleado → panel derecho aparece con perfil del empleado
- Clic en día con panel de empleado abierto → panel de empleado se cierra, panel de día se abre
- Botón X en cada panel lo cierra
- `npx tsc --noEmit` pasa sin errores
</verification>

<success_criteria>
Los dos paneles laterales son funcionales. La administradora puede ver el resumen de cobertura de cualquier día y el perfil de cualquier empleado con un clic desde la planilla.
</success_criteria>

<output>
After completion, create `.planning/phases/03-planilla-principal/03-03-SUMMARY.md`
</output>
