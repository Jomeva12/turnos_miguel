---
phase: 01-fundaci-n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - next.config.ts
  - prisma/schema.prisma
  - prisma/seed.ts
  - prisma/seed.test.ts
  - lib/db.ts
  - lib/utils/format.ts
  - lib/constants/areas.ts
  - app/globals.css
  - app/layout.tsx
  - vitest.config.ts
  - lib/utils/format.test.ts
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-04
  - DATA-05
  - INF-03

must_haves:
  truths:
    - "30 empleados cargados en la base de datos con nombres reales del sistema Laravel"
    - "7 areas cargadas con nombres exactos y colores del sistema Laravel"
    - "Plantillas de turno cargadas con horarios por dia y area, incluyendo comodines"
    - "requiredCount por area/dia coincide con datos_proyecto.md"
    - "Prisma singleton funciona sin pool exhaustion en dev"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Full database schema for all phases"
      contains: "model Employee"
    - path: "prisma/seed.ts"
      provides: "Seed script with employees, areas, shift templates, admin user"
      min_lines: 100
    - path: "prisma/seed.test.ts"
      provides: "Seed verification tests"
      min_lines: 20
    - path: "lib/db.ts"
      provides: "Prisma singleton"
      exports: ["prisma"]
    - path: "lib/constants/areas.ts"
      provides: "Area colors and names as TypeScript constants"
      exports: ["AREA_COLORS"]
    - path: "lib/utils/format.ts"
      provides: "Spanish date formatting utilities"
      exports: ["formatMonthYear", "formatDayShort"]
    - path: "app/globals.css"
      provides: "Tailwind v4 CSS with dark mode variables and glassmorphism"
      contains: "glass"
  key_links:
    - from: "prisma/seed.ts"
      to: "prisma/schema.prisma"
      via: "PrismaClient models"
      pattern: "prisma\\.(employee|area|shiftTemplate)\\."
    - from: "lib/db.ts"
      to: "prisma/schema.prisma"
      via: "PrismaClient singleton"
      pattern: "new PrismaClient"
---

<objective>
Scaffold the Next.js 15 project, create the full Prisma schema, seed all reference data (employees, areas, shift templates), and establish foundational utilities.

Purpose: Every subsequent plan depends on the database schema, seed data, and base utilities. This must be solid before auth or UI work begins.
Output: Working Next.js project with PostgreSQL schema migrated, seed data loaded, and base utilities ready.
</objective>

<execution_context>
@/Users/franciatrasvina/.claude/get-shit-done/workflows/execute-plan.md
@/Users/franciatrasvina/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fundaci-n/01-CONTEXT.md
@.planning/phases/01-fundaci-n/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js project, Prisma schema, and base utilities</name>
  <files>
    package.json, tsconfig.json, next.config.ts, prisma/schema.prisma, lib/db.ts,
    lib/constants/areas.ts, lib/utils/format.ts, app/globals.css, app/layout.tsx,
    vitest.config.ts, lib/utils/format.test.ts
  </files>
  <action>
    1. Initialize Next.js 15 project with `npx create-next-app@latest . --typescript --tailwind --app --src-dir=false --import-alias="@/*"` (accept defaults, overwrite if prompted).

    2. Install all Phase 1 dependencies:
       ```
       npm install prisma @prisma/client better-auth @better-auth/prisma next-themes react-hook-form zod @hookform/resolvers date-fns
       npm install -D vitest @vitest/coverage-v8
       ```
       shadcn init: `npx shadcn@latest init` (select default style, slate base color, CSS variables yes).
       Add components: `npx shadcn@latest add card input button label checkbox badge table`

    3. Set `output: "standalone"` in next.config.ts. Add `serverExternalPackages: ["@prisma/client"]`.

    4. Create `lib/db.ts` — Prisma singleton using the globalThis pattern from research. This MUST be the first data-access file created.

    5. Create `prisma/schema.prisma` with the FULL schema (all models needed through Phase 5, not just Phase 1):
       - User (better-auth managed: id cuid, email unique, name, emailVerified, image, createdAt, updatedAt)
       - Session (better-auth: id cuid, expiresAt, token unique, ipAddress, userAgent, userId -> User)
       - Account (better-auth: id cuid, accountId, providerId, userId -> User, accessToken, refreshToken, etc.)
       - Verification (better-auth: id cuid, identifier, value, expiresAt, createdAt, updatedAt)
       - Employee (id autoincrement, name String, createdAt)
       - Area (id autoincrement, name String unique, color String)
       - EmployeeArea (composite PK: employeeId + areaId)
       - ShiftTemplate (id autoincrement, areaId, dayOfWeek Int 1-7, timeSlot String, isWildcard Boolean, requiredCount Int)
       - Shift (id autoincrement, employeeId, date Date, timeSlot nullable, areaId nullable, isManual Boolean default false; unique on employeeId+date)
       - Absence (id autoincrement, employeeId, startDate Date, endDate Date, type String)
       - GenerationLog (id autoincrement, month Int, year Int, createdAt, notes Json)

       Use `@@map` to keep table names lowercase snake_case if desired. Ensure all relations have proper onDelete cascading.

    6. Create `lib/constants/areas.ts` with AREA_COLORS constant mapping area names to hex colors. Clone the Laravel repo (https://github.com/Jomeva12/yuli_turnos.git) to a temp directory, extract area badge colors from the Blade templates or CSS. If colors are Bootstrap classes, map them to hex equivalents:
       - General: gray (#6b7280)
       - Buffet: amber (#f59e0b)
       - Cosmetico: pink (#ec4899)
       - Domicilios: blue (#3b82f6)
       - Electrodomestico: purple (#8b5cf6)
       - Marking: emerald (#10b981)
       - Varely Camacho: red (#ef4444)
       Update with actual Laravel colors if different from these defaults.

    7. Create `lib/utils/format.ts` with `formatMonthYear(date)` and `formatDayShort(date)` using date-fns v4 with Spanish locale (es). formatMonthYear must capitalize first letter.

    8. Set up `app/globals.css` with Tailwind v4 CSS config:
       - `@import "tailwindcss";`
       - CSS variables for dark mode colors (dark background: hsl(222 47% 11%), foreground: hsl(210 40% 98%))
       - `.glass` class with backdrop-blur-12px, semi-transparent bg, and border
       - All color tokens as CSS custom properties

    9. Update `app/layout.tsx` as minimal root layout with the ThemeProvider from next-themes (attribute="data-theme", defaultTheme="dark", forcedTheme="dark" since this is dark-mode-only). Import globals.css. Set metadata title "GestionTurnos v2.0", lang="es".

    10. Create `vitest.config.ts` for the project. Create `lib/utils/format.test.ts` testing that formatMonthYear capitalizes and returns Spanish month, and formatDayShort returns Spanish day abbreviations (Lun, Mar, Mie, etc.).

    11. Run `npx prisma init` (if not already done), configure DATABASE_URL in .env for local PostgreSQL (postgres://postgres:postgres@localhost:5432/gestion_turnos). Run `npx prisma migrate dev --name init` to create the database.
  </action>
  <verify>
    <automated>npx vitest run lib/utils/format.test.ts --reporter=verbose && npx prisma validate</automated>
  </verify>
  <done>
    - Next.js project builds without errors (`npm run build` succeeds or at least `next lint` passes)
    - Prisma schema validates with all models
    - format.test.ts passes with Spanish month/day output
    - globals.css has glassmorphism variables
    - lib/db.ts exports Prisma singleton
    - lib/constants/areas.ts exports AREA_COLORS with 7 entries
  </done>
</task>

<task type="auto">
  <name>Task 2: Seed script with employees, areas, and shift templates</name>
  <files>prisma/seed.ts, package.json</files>
  <action>
    1. Clone the Laravel repo (https://github.com/Jomeva12/yuli_turnos.git) to /tmp/yuli_turnos. Read `database/seeders/EmployeeSeeder.php` to extract the ~30 employee names. Read `database/seeders/AreaSeeder.php` for area data. Read Blade templates in `resources/views/` for area badge colors.

    2. Create `prisma/seed.ts` that:
       a. Seeds the admin user compatible with better-auth user table: email "yuli@diferencialdx.com", name "Administradora", with a hashed password. MUST use `import { hash } from "better-auth/crypto"` and `await hash(password)`. Do NOT use bcryptjs — better-auth uses scrypt internally, and a bcrypt hash will cause silent login failure. The password is "3176890957a".
       b. Seeds all 7 areas with exact names and colors from the Laravel repo.
       c. Seeds all ~30 employees with names from the Laravel EmployeeSeeder (exact format as in the PHP seeder).
       d. Seeds all shift templates from datos_proyecto.md. Parse each area's schedule into ShiftTemplate records with:
          - areaId (FK to the seeded area)
          - dayOfWeek (1=Monday through 7=Sunday)
          - timeSlot (format: "7:00-11:00" for single, "7:00-11:00|11:30-14:30" for split)
          - isWildcard (true for comodin templates)
          - requiredCount (number of employees needed for that area+day)
       e. Uses upsert pattern so seed is idempotent (can run multiple times safely).

    3. Add `"prisma": { "seed": "npx tsx prisma/seed.ts" }` to package.json. Install tsx as dev dependency: `npm install -D tsx`.

    4. Run `npx prisma db seed` to load the data.

    5. After seeding, run a verification query using prisma studio or a quick script to confirm:
       - Employee count is ~30
       - Area count is exactly 7
       - ShiftTemplate count is reasonable (should be 50+ records covering all area/day combinations)
       - Admin user exists with email "yuli@diferencialdx.com"

    IMPORTANT: Read datos_proyecto.md from the .planning directory to get the exact shift template data. If datos_proyecto.md is not found, check .planning/research/ or the project root. The shift templates must be EXACT — wrong seed data will corrupt every subsequent phase.
  </action>
  <verify>
    <automated>npx tsx -e "const {PrismaClient}=require('@prisma/client');const p=new PrismaClient();(async()=>{const e=await p.employee.count();const a=await p.area.count();const s=await p.shiftTemplate.count();const u=await p.user.count();console.log(JSON.stringify({employees:e,areas:a,templates:s,users:u}));if(e<25||a!==7||s<30||u<1)process.exit(1);await p.\$disconnect();})()"</automated>
  </verify>
  <done>
    - ~30 employees loaded with real names from Laravel seeder
    - 7 areas loaded with correct names and hex colors
    - Shift templates loaded covering all area/day combinations with correct requiredCount
    - Admin user exists with email yuli@diferencialdx.com
    - Seed is idempotent (can run twice without errors)
  </done>
</task>

<task type="auto">
  <name>Task 3: Seed verification test (Wave 0 Nyquist)</name>
  <files>prisma/seed.test.ts</files>
  <action>
    Create `prisma/seed.test.ts` to verify seed data integrity. This is a Wave 0 test file required by the Nyquist rule — it must exist so that subsequent verification commands have something to run.

    The test should:
    1. Import PrismaClient from `@prisma/client`
    2. Create a test suite "Seed Data Verification" with tests:
       - "should have approximately 30 employees" — query `prisma.employee.count()`, assert >= 25 and <= 35
       - "should have exactly 7 areas" — query `prisma.area.count()`, assert === 7
       - "should have shift templates for all areas" — query `prisma.shiftTemplate.count()`, assert >= 30
       - "should have admin user" — query `prisma.user.findUnique({ where: { email: "yuli@diferencialdx.com" } })`, assert not null
       - "should have correct area names" — query all areas, assert names include General, Buffet, Cosmetico, Domicilios, Electrodomestico, Marking, Varely Camacho
    3. Use beforeAll/afterAll to create and disconnect PrismaClient
    4. These are integration tests that run against the real database (DATABASE_URL from .env)
  </action>
  <verify>
    <automated>npx vitest run prisma/seed.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - prisma/seed.test.ts exists with 5 test cases
    - All tests pass against seeded database
    - Employee count ~30, area count 7, templates >= 30, admin user exists
  </done>
</task>

</tasks>

<verification>
- `npx prisma validate` passes
- `npx vitest run --reporter=verbose` passes all tests (format + seed)
- Database contains correct seed data counts
- `npm run build` compiles without errors (or `npx next build` if script not yet configured)
</verification>

<success_criteria>
- Prisma schema contains all models for phases 1-5
- Seed data matches Laravel source exactly
- Admin user hashed with better-auth/crypto (scrypt), NOT bcryptjs
- Prisma singleton pattern established
- Spanish formatting utilities working
- Dark mode CSS variables and glassmorphism ready
- Project builds and tests pass
- prisma/seed.test.ts validates seed integrity
</success_criteria>

<output>
After completion, create `.planning/phases/01-fundaci-n/01-01-SUMMARY.md`
</output>
